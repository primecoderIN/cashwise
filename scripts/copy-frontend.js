const fs = require("fs"); const path = require("path"); const frontendDist = path.join(__dirname, "../apps/frontend/dist"); const backendPublic = path.join(__dirname, "../apps/backend/public"); if (fs.existsSync(backendPublic)) { fs.rmSync(backendPublic, { recursive: true, force: true }); } fs.cpSync(frontendDist, backendPublic, { recursive: true }); console.log("Frontend build copied to backend/public");
